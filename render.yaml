services:
  - type: web
    name: debit-card-generator
    env: node
    rootDir: web_card_app
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
